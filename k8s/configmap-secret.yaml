apiVersion: v1
kind: ConfigMap
metadata:
  name: db-config
data:
  DB_HOST: "postgres-service"
  DB_PORT: "5432"
  DB_NAME: "appdb"
  DB_USER: "appuser"

---
apiVersion: v1
kind: Secret
metadata:
  name: db-secret
type: Opaque
data:
  # "apppassword" encodé en base64 -> echo -n "apppassword" | base64
  DB_PASSWORD: "YXBwcGFzc3dvcmQ="